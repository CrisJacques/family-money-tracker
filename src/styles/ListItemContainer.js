import styled from "styled-components";

/**
 * O objetivo deste styled component é estilizar todas as listas da aplicação
 */
const ListItemContainer = styled.div`
  list-style-type: square;
  padding: 1.2em;
`;
export default ListItemContainer;
