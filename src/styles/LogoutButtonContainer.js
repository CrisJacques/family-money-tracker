import styled from 'styled-components';

/* O objetivo deste styled component é estilizar o botão Sair do menu lateral, para que ele fique com o mesmo aspecto visual dos demais itens do menu, por questão de consistência visual */
const LogoutButtonContainer = styled.button`
    background-color: white;
    border: none;
    cursor: pointer;
`;

export default LogoutButtonContainer;