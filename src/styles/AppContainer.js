import styled from "styled-components";

/* O objetivo deste styled component é garantir um tamanho máximo para as telas do aplicativo e mantê-lo centralizado em telas muito grandes, seguindo a estratégia mostly fluid */
const AppContainer = styled.div`
  max-width: 1438px;
  margin: auto;
`;

export default AppContainer;
