import styled from "styled-components";

/* O objetivo deste styled component é garantir que o header da tela de login (logo e nome do app) fique centralizado */
const LoginHeaderContainer = styled.div`
  text-align: center;
`;

export default LoginHeaderContainer;
